'use strict';
module.exports.image = function () {
    // var config = require('./../config'),
    //     bits = slug.split('/'),
    //     slug = bits[1],
    //     type = bits[0],
    //     sizes = config.media.sizes,
    //     images = config.media.images;


    // ;
    // this.context.image = { src: 'src/to/image', alt: 'Some alt' };
    // console.log(this);
    return '';
};
